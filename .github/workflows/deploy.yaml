name: 'Deploy App to S3'

# on: [push] # toutes les branches
on:

  push:
    branches:

      - pichat # uniquement sur la branche main


jobs:
  deployemen: # non
    runs-on: ubuntu-latest # instance temp
    steps:
    - name: checkout code
      uses: actions/checkout@v4

    - name: List directory
      run: |
       ls -la

    - name: List directory
      run: |
       touch newfie.txt
 
